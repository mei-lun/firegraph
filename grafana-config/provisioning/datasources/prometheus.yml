# Grafana 数据源自动配置（provisioning）
# 启动时自动加载，无需在 UI 手动添加
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "15s"
